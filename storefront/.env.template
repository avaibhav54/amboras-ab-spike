# Medusa Backend Orchestrator
NEXT_PUBLIC_MEDUSA_BACKEND_URL=http://localhost:9000

# Store Environment ID (set by dev orchestrator)
NEXT_PUBLIC_STORE_ID=

# Publishable API Key (inherited from template DB)
NEXT_PUBLIC_MEDUSA_PUBLISHABLE_KEY=

# Stripe Public Key (optional — fetched from backend if not set)
NEXT_PUBLIC_STRIPE_PUBLIC_KEY=

# PayPal client_id is fetched per-tenant at runtime from Medusa's
# /store/paypal-connect endpoint. No env var needed here — the storefront
# resolves the right merchant's PayPal app via the publishable key above.

# Analytics endpoint (orchestrator backend URL)
NEXT_PUBLIC_ANALYTICS_ENDPOINT=
